apiVersion: v1
kind: Service
metadata:
  name: {{ include "raysh.io.strategy-api.fullname" . }}
  labels:
    {{- include "raysh.io.strategy-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "raysh.io.strategy-api.selectorLabels" . | nindent 4 }}

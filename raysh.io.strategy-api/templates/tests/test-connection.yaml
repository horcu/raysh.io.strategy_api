apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "raysh.io.strategy-api.fullname" . }}-test-connection"
  labels:
    {{- include "raysh.io.strategy-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "raysh.io.strategy-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
